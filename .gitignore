node_modules/
*.log
*.db
.DS_Store
docs/superpowers/

# Build output — this is what you load into Chrome. Rebuild with `npm run build`.
dist/

# Test and tooling artifacts
coverage/
.npm-cache/

# Chrome Web Store packages — rebuilt with `npm run package`.
release/

# Personal settings exports — may contain internal/company URLs.
# Import these through the extension's settings page instead of committing them.
*.local.json
